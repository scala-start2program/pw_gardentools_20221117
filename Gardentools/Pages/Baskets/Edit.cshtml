@page
@model Gardentools.Pages.Baskets.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Basket</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Basket.Id" />
            <div class="form-group">
                <label asp-for="Basket.UserId" class="control-label"></label>
                <select asp-for="Basket.UserId" class="form-control" asp-items="ViewBag.UserId"></select>
                <span asp-validation-for="Basket.UserId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Basket.ArticleId" class="control-label"></label>
                <select asp-for="Basket.ArticleId" class="form-control" asp-items="ViewBag.ArticleId"></select>
                <span asp-validation-for="Basket.ArticleId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Basket.Count" class="control-label"></label>
                <input asp-for="Basket.Count" class="form-control" />
                <span asp-validation-for="Basket.Count" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
